{
  "name": "backend",
  "version": "1.0.0",
  "description": "Lambda backend API with TypeScript",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "start:local": "npm run build && sam local start-api --port 5000",
    "dev": "npm run build && sam local start-api --port 5000 --warm-containers EAGER"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.4.5",
    "typescript": "^5.1.6",
    "vitest": "^1.6.0"
  }
}
